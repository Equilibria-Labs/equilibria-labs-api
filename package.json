{
  "name": "equilibria-labs",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm exec vercel dev",
    "build": "turbo run build",
    "build:dry": "turbo run build --dry",
    "build:web": "turbo run build --filter=@equilibria-labs/web",
    "build:web-verbose": "TURBO_LOG_VERBOSITY=verbose turbo run build --filter=@equilibria-labs/web",
    "start": "turbo run start",
    "lint": "turbo run lint --parallel",
    "dev:web": "turbo run dev --filter=@equilibria-labs/web",
    "dev:api": "turbo run dev --filter=@equilibria-labs/api",
    "dev:all": "turbo run dev --parallel",
    "build:types": "cd packages/shared-types && pnpm run build",
    "clean": "rm -rf node_modules apps/*/node_modules packages/*/node_modules || true",
    "clean:build": "rm -rf packages/shared-types/dist apps/web/.next apps/api/dist || true",
    "clean:all": "pnpm run clean && pnpm run clean:build && rm -f pnpm-lock.yaml || true",
    "fresh-install": "pnpm run clean:all && pnpm install"
  },
  "devDependencies": {
    "turbo": "^1.12.4",
    "typescript": "5.1.6"
  },
  "dependencies": {
    "@supabase/auth-helpers-nextjs": "0.10.0",
    "@supabase/auth-helpers-shared": "^0.7.0",
    "@supabase/ssr": "^0.0.10",
    "@types/node": "^20.0.0",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "next": "14.0.4"
  },
  "engines": {
    "node": ">=18.17.0",
    "pnpm": ">=7"
  },
  "pnpm": {
    "peerDependencyRules": {
      "allowedVersions": {
        "react": "18",
        "react-dom": "18"
      }
    }
  }
}
